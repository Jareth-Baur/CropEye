
## Getting Started
First, clone the project:
```bash
git clone https://github.com/Jareth-Baur/CropEye

cd CropEye
```

then run the development server:

```bash
npm install 

npm run dev
```
Open [http://localhost:3000](http://localhost:3000)